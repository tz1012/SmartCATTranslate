# 변경 기록

이 파일에는 사용자에게 보이는 기능과 동작 변경을 기록합니다.

## 미출시

### SmartCAT Translate 0.1.8

- 시크릿 번역 옵션을 각 작업 화면에서 제거하고 `설정 → 개인정보·기록`의 공통 옵션으로 이동했습니다.
- 창 높이를 최소 크기로 줄여도 원문 글자 수가 하단 작업 버튼 뒤로 잘리지 않도록 번역 입력 영역이 남은 높이에 맞춰 축소됩니다.
- 번역 또는 취소 처리 중의 설정 잠금 안내를 `기록` 탭과 겹치지 않는 전용 영역에서 최대 두 줄로 표시합니다.
- 중복된 우측 상단 `A` 계정 버튼을 제거하고 알림 버튼을 헤더 맨 오른쪽으로 옮겼습니다. 계정 정보와 설정은 기존처럼 햄버거 메뉴에서 이용할 수 있습니다.

### SmartCAT Translate 0.1.7

- 다음 버전부터 앱의 `업데이트` 버튼으로 서명 검증된 업데이트를 설치할 수 있도록 Tauri updater 공개키와 공개 GitHub 업데이트 피드를 내장했습니다.
- 기존 0.1.6에는 updater 공개키가 없으므로 0.1.7은 한 번 수동 설치하는 전환 버전입니다.

### SmartCAT Translate 0.1.6

- 암호화 기록 일부가 손상되어도 읽을 수 있는 번역 기록은 계속 표시하고, 건너뛴 기록 수를 안전한 안내 문구로 알립니다.
- 텍스트 번역 결과가 있을 때 앱 안에서 `Ctrl+Enter`를 누르면 새 번역을 시작하지 않고 결과를 클립보드에 복사합니다.
- 빠른 번역 팝업의 시크릿 번역 옵션을 프로필 옆의 작은 옵션으로 옮겼습니다.
- `Ctrl+C`로 끝나는 빠른 번역 단축키는 이미 복사된 선택문을 사용하여 불필요한 추가 복사 때문에 번역이 실패하지 않도록 수정했습니다.

- 상단 메뉴를 전환해도 입력 중인 텍스트, 완료된 번역문, 선택한 문서와 문서 번역 진행 상태가 유지됩니다.
- 상단 알림마다 `확인` 버튼을 추가해 확인한 알림이 현재 앱 실행 중 다시 표시되지 않도록 했습니다.
- 텍스트 번역 기록은 실제 저장 성공 뒤에만 완료 처리하며, 저장 실패를 안전한 문구로 알리고 같은 기록을 다시 저장할 수 있습니다. 기록 목록을 불러오지 못한 경우에도 다시 시도할 수 있습니다.
- 텍스트 번역 하단의 작업 버튼과 상태 문구를 한 줄로 배치하고 남는 세로 공간을 번역 입력 영역에 사용하도록 조정했습니다.
- 번역 또는 취소 처리 중 표시되는 설정 잠금 안내를 본문에서 상단 `기록` 메뉴 옆의 작은 한 줄 문구로 옮겨 화면 높이가 변하지 않도록 했습니다.
- 설정 화면을 열 때 이전 작업 화면이 아래에 함께 노출되지 않도록 숨김 처리를 바로잡고, `Esc` 키로 설정을 닫아 이전 화면으로 돌아갈 수 있게 했습니다.
- 단축키와 차단 프로그램을 별도로 저장한 뒤 전체 설정을 저장해도 이전의 빈 목록이 덮어쓰지 않도록 설정 상태를 동기화했습니다.

### SmartCAT Translate 0.1.5

- 앱 시작 알림과 설정의 업데이트 화면에 `업데이트` 버튼을 제공하며, 한 번 누르면 서명된 업데이트를 다운로드·검증·설치한 뒤 앱을 재시작합니다. 서명되지 않은 공개 릴리스는 자동 실행하지 않고 기존처럼 릴리스 페이지만 엽니다.

### SmartCAT Translate 0.1.4

- 앱 시작 시 새 버전의 존재 여부만 확인해 상단 알림에 표시합니다. 서명된 updater가 구성된 배포는 기존 검증·설치 승인 흐름을 사용하고, 그렇지 않은 배포는 공개 GitHub Release 페이지만 열어 사용자가 직접 내려받도록 합니다. 백그라운드 다운로드·설치·재시작은 하지 않습니다.
- 단축키 녹화를 시작하면 입력 영역으로 포커스를 옮기고, 키를 누르는 순간 수정키 상태를 기록해 Ctrl·Alt·Shift를 먼저 놓아도 조합이 사라지지 않게 했습니다. 새 녹화를 시작한 뒤 늦게 도착한 이전 충돌 검사 결과도 현재 입력을 덮어쓰지 않습니다.
- 입력 뒤 즉시 붙여넣기 번역을 시작한 경우 대기 중이던 자동 번역이 같은 문장을 다시 전송하지 않도록 수정했습니다. 앱 정보는 설치된 패키지 버전을 직접 표시합니다.

### SmartCAT Translate 0.1.3 로컬 핫픽스

- 이미지 파일을 선택하거나 화면 캡처를 확인하면 OCR·번역을 자동으로 시작하고, 완료되면 `텍스트` 화면으로 이동해 인식 원문 전체를 왼쪽에, 번역문 전체를 오른쪽에 표시합니다. 이미 완료된 캡처 번역을 다시 요청하거나 텍스트 기록으로 중복 저장하지 않으며, 원문을 편집하면 전달된 번역 결과를 즉시 비웁니다.
- 같은 원본 준비 이벤트가 연속 도착해도 진행 중인 OCR·번역은 한 번만 시작하며, 실패가 끝난 뒤에는 정상적으로 다시 시도할 수 있습니다.
- 캡처 오버레이의 실제 화면 경계를 운영체제 DPI 배율에 맞춰 물리 좌표와 논리 좌표 사이에서 변환해, 배율이 다른 환경에서도 선택 영역이 정확히 대응하도록 수정했습니다.
- 확인 동작을 중복 제거해 같은 선택이 여러 번 완료 처리되지 않도록 했고, 사용자에게는 allowlist를 통과한 정제된 정확한 지원 코드만 표시합니다.
- 숨김 캡처 오버레이는 세션 범위에서만 생성하고 세션 종료 시 해제하며, 네이티브 창 닫기는 진행 중인 선택을 취소하도록 연결했습니다.
- listener 준비 완료를 확인한 뒤 캡처를 시작하고, 준비 전에 도착하는 초기 이벤트도 잃지 않도록 경쟁 조건을 처리했습니다.
- 유효하지 않은 선택은 작업을 소진하지 않고 다시 시도할 수 있으며, 관련 경계를 회귀 테스트로 고정했습니다.
- Windows 제한 토큰에서 작업용 임시 폴더를 만든 직후 재정규화가 실패하던 문제를 제거했습니다. 작업 ID가 정확한 32자리 16진수인지, 생성 경로가 보호된 루트의 직접 자식인지, 재분석 지점이 아닌지를 확인해 경로 이탈은 계속 차단합니다.
- PNG 저장 실패를 인코딩, 저장소 열기, 저장소 루트, 쓰기 단계별 지원 코드로 구분하고, 일시적인 파일 열기 거부만 최대 세 번 재시도하며 기존 파일은 덮어쓰지 않습니다.
- OCR 구조화 번역에서 안전한 내부 `reasoning` 진행 항목을 도구 실행으로 오인하던 문제를 수정했습니다. 명령 실행, 동적 도구 호출, 서버 요청 및 알 수 없는 항목은 계속 실패로 닫힙니다.
- 새 의존성을 추가하지 않았고 URL, 비밀정보, 사용자 콘텐츠 또는 민감한 원문·번역문을 로그에 기록하지 않습니다.
- 설치된 0.1.3 앱에서 화면 선택, 오버레이 종료, OCR, `텍스트` 탭 자동 전환, 원문·번역문 자동 채움까지 로컬 실사용 검증을 완료했습니다. GitHub Actions는 실행하지 않습니다.
- 최종 로컬 검증은 AccountPanel 15/15, `codex_auth` 23/23, `codex_bootstrap` 4 passed / 2 ignored, Rust lib 100/100, 전체 프런트엔드 97/97, translation coordinator 40/40이며 build, check, fmt, records, privacy 및 diff 검사도 통과했습니다.
- 최종 Windows NSIS 설치 파일 SHA-256은 `D14E879CFC60E437328BC96122D12626C479A19F09EF488527DAFC6615501197`, 설치 앱은 `6EA1CC73831FDF9EED696F26789A91C9FB0201D4A92DD9B6F38EDFE976138464`이며 Authenticode 상태는 `NotSigned`입니다.

### SmartCAT Translate 0.1.2 화면 캡처 복구

- 트레이 수명주기의 닫기 가로채기를 `main`과 `quick-popup`에만 적용해 캡처 오버레이가 확인 또는 취소 뒤 정상적으로 닫히며, 회귀 테스트로 이 label 경계를 고정했습니다.
- 확인·취소 버튼 label에 읽기 쉬운 명시적 대비를 적용하고 실제 CSS computed style 테스트로 검증했습니다.
- 새 의존성을 추가하지 않았고 URL, 비밀정보, 사용자 콘텐츠를 로그에 기록하지 않습니다.
- 사용자 요청에 따라 GitHub Actions는 의도적으로 실행하지 않으며, 로컬 검증과 설치를 릴리스 관문으로 사용합니다.
- 캡처 형태의 JSON은 명시적으로 번역 데이터로 취급해 사람 언어 값만 번역하고 식별자와 구조는 보존하며, 사고 대응·CI·명령처럼 보이는 텍스트도 절대 실행하지 않습니다.
- 도구를 사용할 수 없을 때 실패로 닫히는(no-tools fail-closed) 경계는 그대로 유지됩니다.
- 캡처·가져오기·번역 오류는 단계별로 정제된 지원 코드로 표시하며, 원본 준비 완료 상태는 재시도할 수 있게 유지합니다.

### SmartCAT Translate 0.1.1 복구

- 고정된 Codex 0.144.4 번역 프로토콜의 정상 진행 알림 `deprecationNotice`, `account/rateLimits/updated`, `thread/tokenUsage/updated`는 필수 공식 필드·타입·scope를 검증하면서 호환 가능한 추가 필드는 허용합니다. `modelContextWindow`는 선택 필드로서 생략, `null`, signed i64를 허용하고, 필수 token breakdown 정수는 signed i64 범위를 벗어나면 거부합니다. 같은 이름의 malformed payload와 알 수 없는 이벤트는 기존 thread/turn 범위 검증과 함께 실패 폐쇄합니다.
- listener 등록 실패 뒤 Retry는 주 작업 영역과 빠른 팝업 모두에서 새 guarded 등록 시도를 수행합니다. UI에는 정확한 지원 코드 allowlist 값만 표시하고, 알 수 없거나 안전하지 않은 값은 `translation_start_failed`로 대체합니다.
- 정보 화면은 설치된 앱 버전 0.1.1을 표시하며, 이를 설치된 런타임 버전이라고 부르지 않습니다. 앱 버전 조회가 실패하면 raw rejection 대신 현지화되고 정리된 unavailable 메시지를 표시합니다.
- 부트스트랩 테스트의 가짜 프로세스가 하드코딩된 버전 대신 현재 패키지 버전을 검증하도록 바꿔 이후 버전 갱신에도 같은 회귀를 방지합니다. 이는 테스트 지원 변경이며 프로덕션 런타임 동작은 바꾸지 않습니다.
- 최종 로컬 검증은 AccountPanel 15/15, `codex_auth` 23/23, `codex_bootstrap` 4 passed / 0 failed / 2 ignored, Rust lib 86/86, frontend 81/81, translation coordinator 38/38을 통과했습니다. production build, `cargo check`, `cargo fmt -- --check`, `git diff --check`도 통과했습니다.
- 최종 NSIS 설치 관리자 SHA-256은 `906FE7067ACC34E99751F6C88BB0E88ED2A37D90DBDA6AE4BE21A0C1C5843A28`, 설치된 앱은 `8DC37627FE470E7099876C068D5DA864DD8CF3A9123A5F22A929ADF7894E67C1`, 설치된 sidecar는 `E4DB53E141020CBB70D0CBDDC4BE796FCC12CFE027A6A1F7DEAC70B959599D53`이며 Authenticode는 `NotSigned`입니다.
- NSIS in-place 설치는 exit 0이었고, 실제 설치 앱의 정보 화면에서 0.1.1을 확인했으며 고정된 비민감 번역 smoke가 통과했습니다. 이후 Exit 메뉴로 종료했을 때 앱과 sidecar 프로세스가 남지 않았습니다.
- 사용자 지시에 따라 GitHub Actions는 의도적으로 실행하지 않았고 로컬 검증과 설치를 최종 관문으로 사용했습니다. GitHub CI가 통과했다고 주장하지 않습니다.
- URL, token, 계정 식별자, 이메일, 원문 또는 번역 결과는 기록하지 않았습니다.

### 최종 검증 증거

- 최종 커밋 `d2ea38b`의 GitHub Actions push run `33478909612`와 PR run `33478914106`은 작업 단계가 시작되기 전에 거부되었습니다. 샘플링한 모든 annotation은 최근 계정 결제 실패 또는 Actions 지출 한도로 runner가 시작되지 않았다고 기록했습니다. 이는 코드·테스트 실패가 아닌 외부 GitHub billing 상태이며, 최종 GitHub CI가 통과했다고 주장하지 않습니다.
- 이전 `ff9f114` push run `33471970202`는 privacy, records, Windows/macOS frontend 및 Windows/macOS Rust를 통과했습니다. PR package run은 감사된 패치 Codex release build를 완료하는 단계까지 도달해 48분 17초 만에 LF 패치 해시 수정을 입증했고, 이후 `cargo-cyclonedx` 누락을 드러냈습니다. 최종 커밋에서 pin/order/timeout 회귀 테스트와 함께 해당 프로비저닝을 수정했습니다.
- `d2ea38b` 제품 트리는 OMX를 통해 감사된 Windows sidecar와 manifest 일치 상태로 로컬 패키징했습니다. MSI는 125063168 bytes, SHA256 `7677D1B188F5D594409DF768AB6502BF29B146C75D1ED2D48DB801654378ABDA`, NSIS는 84485776 bytes, SHA256 `5A3E157F172D8F3A36C353BAE452AA547AC48C7DB6DD2688DFE789AA4315BC16`이며 둘 다 예상한 NotSigned prerelease입니다.
- 기존 D 드라이브 설치 위치에 NSIS를 in-place 재설치한 결과 exit 0이었고 app data는 삭제되지 않았습니다. 설치 앱 SHA256은 `6F49CDCF5E8A509282F89E4F34E26169B82B0FDD1CFAC9723C6AD813105682AE`, 설치 sidecar SHA256은 `E4DB53E141020CBB70D0CBDDC4BE796FCC12CFE027A6A1F7DEAC70B959599D53`입니다. sidecar는 `codex-cli 0.144.4`, manifest exact match, patch SHA `ffe6e77bea83a860259000332eb6d2c256bdc5def3375447319dc055c7f5bcaf`이며, 실행 후 main window와 child sidecar가 안정적으로 유지되었습니다.
- 설치 증거는 Git 외부 `C:\\Users\\cyary\\.paseo\\install-artifacts\\d2ea38bcdc736673532efe5e0c4658477ad5dadb`에 보존했습니다. 인증 URL, token, account data, source text 및 user document content는 기록하지 않습니다.

### 추가

- 사용자가 설정에서 직접 확인한 뒤 변경 내용·게시일·크기를 검토하고, 15분 동안 해당 버전에만 유효한 한 번의 승인으로 다운로드와 서명 확인을 거쳐 재시작을 다시 확인한 경우에만 설치하는 업데이트 화면을 추가했습니다.
- Windows x64 MSI/NSIS, macOS Intel/Apple Silicon 앱·DMG를 보호된 환경에서 서명·공증하고 모든 플랫폼 성공 뒤 초안 릴리스를 만드는 GitHub Actions, 체크섬·SBOM·라이선스·provenance 관문을 추가했습니다.
- 사용자 문서를 건드리지 않는 disposable root 기반 로컬 unsigned 패키징/짧은 설치 앱 acceptance 스크립트와 로그인·텍스트·단축키·캡처·문서·기록·복구·업데이트 수동 체크리스트를 추가했습니다.
- 운영체제 자격 증명 저장소의 키로 원문·번역문·파일 표시 이름을 암호화하는 로컬 기록과 보관 기간, 복사·개별/전체 삭제 화면을 추가했습니다.
- 텍스트, 빠른 팝업, 이미지·화면, 문서에 공유 시크릿 번역을 추가했습니다. 시크릿 작업은 기록과 재시작 복구에 저장하지 않습니다.
- 중단된 문서 번역을 7일 동안 암호화해 보존하고 파일 fingerprint와 전체 번역 옵션이 그대로인 경우에만 계속하는 복구 안내를 추가했습니다.
- root-confined 임시 데이터 정리, 개인정보 안전 진단 형식과 token·이메일·사용자 경로·canary 검사 관문을 추가했습니다.
- 텍스트·스캔·혼합 PDF를 페이지별로 분류하고 Windows/macOS 로컬 OCR과 좌표 기반 번역 오버레이로 원본을 보존한 새 PDF 사본을 만드는 기능을 추가했습니다.
- DOCX/PPTX/XLSX/PDF 공통 문서 화면에 언어·모델·프로필·용어집, 형식별 서식 옵션, 출력 폴더, 단계 진행·취소·재시도, 파일/폴더 열기와 페이지·슬라이드·시트·셀별 위험 보고를 추가했습니다.

### 수정

- GitHub PR 실행 `33471973155`의 macOS ARM 패키지 작업 `99743800112`에서 감사 패치와 해시 검증 및 Codex 릴리스 컴파일은 48분 17초 만에 통과했지만, SBOM 생성 직전에 `cargo cyclonedx`가 없어 `build-smartcat-codex-runtime: sbom_command_failed`로 실패한 회귀를 수정했습니다. 같은 도구 누락을 겪을 Windows와 macOS Intel 작업은 취소된 상태였습니다.
- PR 전용 `package_unsigned` 작업이 비용이 큰 `pnpm runtime:build`보다 먼저 `cargo-cyclonedx` 정확한 버전 0.5.9를 `--locked`로 설치하도록 릴리스 workflow의 고정 방식을 재사용했고, 작업이 무기한 정체되어 보이지 않도록 120분 제한을 추가했습니다. 런타임 패치와 핀은 변경하지 않았습니다.
- 회귀 검사는 PR 패키지 작업 범위만 읽어 고정 버전과 `--locked`, SBOM 도구 설치와 런타임 빌드의 순서, 120분 제한을 확인합니다. `pnpm runtime:build:test` 21/21, `pnpm records:test` 22/22, `pnpm records:check`, actionlint 1.7.12의 CI·런타임 릴리스 workflow 검사, `git diff --check`로 검증했습니다.

- 강제 푸시 뒤 이전 기준 커밋이 원격 참조에서 사라져도 CI 기록 검사가 기본 브랜치의 도달 가능한 커밋을 대체 기준으로 사용하도록 수정했습니다. 명시 기준과 대체 기준이 모두 유효하지 않으면 검사를 중단하며, 초기 푸시의 빈 트리 기준 동작은 유지합니다.
- SmartCAT 런타임 릴리스 workflow가 작업 생성 전에 거부되지 않도록 러너 임시 경로 환경 변수를 허용된 빌드 단계 범위로 옮겼습니다. 작업 수준 환경 변수에서 러너 컨텍스트를 다시 사용하지 못하게 하는 회귀 검사도 추가했습니다.
- macOS Intel·Apple Silicon 빌드에서 투명 화면 캡처 창과 단축키 충돌 확인 구현이 컴파일되도록 Tauri 기능 설정과 macOS probe 생성자를 보완했습니다. CI의 프런트엔드 검사가 PR 전용 설치 패키징 작업을 일반 화면 작업으로 오인하지 않도록 검사 범위도 바로잡았습니다.
- 원문 입력란에 텍스트를 붙여넣으면 번역 버튼을 누르지 않아도 즉시 번역을 시작하도록 수정했습니다. ChatGPT 기록을 저장하지 않는 설정에서도 작업별 임시 대화를 안전하게 만들어 실제 번역 요청이 중단되지 않습니다.
- 기존 Codex/ChatGPT 계정 인증 파일을 현재 형식으로 안전하게 가져오고, Codex의 최신 `read-only` 프로토콜 값으로 번역 세션을 시작해 로그인 후 계정 상태 확인과 번역 준비가 정상 완료되도록 수정했습니다.
- Windows에서 SmartCAT Translate와 bundled Codex를 실행할 때 검은 터미널/콘솔 창이 나타나지 않도록 수정했습니다.
- 임시 정리·보존 알림을 본문 배너 대신 상단 종 아이콘에서 확인하도록 바꾸고, 작은 창에서도 번역 작업 버튼과 메뉴 하단 항목이 보이도록 고정했습니다.
- 화면 캡처 overlay가 운영체제 창 포커스를 다시 받도록 수정해 Enter·Escape·방향키를 전달하며, 키보드를 사용할 수 없는 경우에도 확인·취소 버튼으로 완료하거나 닫을 수 있게 했습니다.
- updater 공개키 검증 도구가 별도 바이너리로 추가된 뒤에도 Tauri 패키징이 데스크톱 앱 `smartcat-translate`를 기본 실행 대상으로 명확히 선택하도록 수정했습니다.
- 설치 앱 acceptance 정리가 CI 여부만으로 기존 데이터를 지우지 않도록 강화했습니다. 기존 앱 데이터·자격 증명·설치를 먼저 확인하고, 이번 실행이 직접 만든 정확한 경로와 설치·앱 사본만 소유권 플래그로 정리합니다.
- Windows 업데이트 메타데이터가 Tauri v2의 서명된 NSIS ZIP(우선) 또는 MSI ZIP만 가리키고 원시 설치 파일은 사용하지 않도록 수정했습니다. `latest.json`은 릴리스 자산 안에서 먼저 생성되어 동일 체크섬·attestation·업로드 대상이 됩니다.
- 배포 앱의 acceptance 전용 고정 키와 데이터 경로 우회를 제거했습니다. GitHub의 일회용 러너에서만 실제 Credential Manager/Keychain과 기본 앱 데이터로 설치 앱을 확인하고, 로컬 스크립트는 설치·실행하지 않습니다.
- 새 버전의 초기 lifecycle·계정·설정·개인정보 상태·기록·복구 로드가 모두 성공하고 안정 시간이 지난 뒤에만 LKG를 기록합니다. SPDX 식과 CycloneDX 1.6 SBOM 구조도 유지 관리되는 파서·검증기로 실패 폐쇄합니다.
- 릴리스가 정확한 기존 `app-vSEMVER` 태그와 네 버전 표기의 일치를 먼저 확인하고, macOS Intel은 지원 중인 macOS 15 Intel 러너에서 빌드하도록 강화했습니다. 보호된 비밀에 접근한 뒤에는 updater 키 쌍 canary 검증, 실제 플랫폼 서명·공증·staple 및 일회용 루트의 설치 앱 준비 확인이 모두 끝나야 초안 릴리스를 만듭니다.
- 업데이트 설치 전 상태를 이전/대상 버전과 설치 관리자 참조로 남기고, 새 버전의 프런트엔드가 hydration과 주 창 준비를 완료한 경우에만 last-known-good로 승격하도록 수정했습니다. 재시작 승인은 확인 직후 백엔드가 버전·설치 승인에 결합해 한 번만 소비합니다.
- SBOM에 npm/Cargo 라이선스 식을 포함하고 배포 의존성의 실제 LICENSE/NOTICE 문서를 묶으며, 문서가 없는 예외는 이유와 라이선스를 명시하지 않으면 릴리스를 실패하도록 강화했습니다.
- 개발/로컬 빌드는 업데이트를 실패 폐쇄하며, 보호된 릴리스 환경이 실제 GitHub 저장소와 공개키를 임시 구성한 경우에만 업데이트를 허용합니다. 이전 설치 관리자와 마지막 정상 시작 지점을 안내하되 자동 롤백은 실행하지 않습니다.
- 같은 임시 저장소를 여는 cleanup 서비스들이 하나의 process-wide 잠금과 pending 상태를 공유하도록 바꾸고, 시작할 때 primary·temp 목록과 실제 UUID 작업 폴더를 항상 합쳐 정리 대상을 놓치지 않도록 강화했습니다.
- 문서 작업의 완료 상태는 저장 단계를 실제로 마친 경우에만 허용하고, 임시 정리 재시도 목록은 같은 폴더의 동기화된 임시 파일을 원자 교체해 저장합니다. 손상된 목록은 유효한 임시 파일 또는 남은 UUID 작업 폴더에서 보수적으로 복구합니다.
- 저장된 기록 보관 기간을 읽기 전에는 자동 삭제하지 않으며, 잘못된 설정이나 임시 파일 정리 실패가 있으면 보수적으로 보존하고 개인정보 상태 안내를 표시하도록 강화했습니다.
- 재시도 가능한 문서 작업은 일시정지 상태로 보존하고, 성공·취소·복구 불가 실패는 각각 완료·취소·실패 상태를 확정한 뒤 복구 정보를 제거합니다. 시크릿 작업은 같은 앱 실행 중에만 메모리에서 이어갈 수 있음을 명확히 안내합니다.
- 운영체제 키와 암호화 저장소가 zeroize되는 키 버퍼를 직접 소유하도록 강화하고, 임시 파일 삭제 실패는 내용이나 경로 없이 재시도 목록에 기록해 다음 시작 시 다시 정리합니다.
- 문서 번역의 모든 단계와 완료 batch를 실행 중 즉시 암호화 저장해 갑작스러운 종료 뒤에도 마지막 확정 지점부터 복구하도록 강화했습니다. 완료·취소 시 복구 정보와 임시 데이터를 함께 제거합니다.
- 문서와 이미지 임시 파일을 하나의 사용자 전용 저장소 아래 UUID 작업 폴더로 통합하고 시작 시 7일 이상 남은 작업을 제거하며 symlink와 Windows reparse point를 거부합니다.
- 번역 기록의 30일 보존을 시작·저장·목록 조회에 적용하고 같은 시각의 기록도 빠뜨리지 않는 cursor를 사용합니다. 복구 안내에는 중단 후 경과 시간을 표시합니다.
- 운영체제 키 처리의 임시 Base64·decoded buffer 정리를 강화하고 자격 증명 저장소를 사용할 수 없을 때 native 안내 후 실패 폐쇄하도록 수정했습니다.
- 개인정보 검사가 누락되거나 너무 큰 입력을 성공으로 간주하지 않고 실제 sanitized captured log와 변경 기록을 검사하도록 강화했습니다.

- PDF의 ToUnicode·변환 행렬·CropBox·회전 좌표 처리를 보강하고 안전하게 해석할 수 없는 페이지는 로컬 OCR로 전환하도록 수정했습니다.
- 스캔·혼합 PDF의 큰 이미지 판정, native/OCR 중복 제거, 300DPI 래스터 fallback과 낮은 신뢰도·글꼴 대체·overflow 경고를 보강했습니다.
- 번역 PDF를 동기화된 새 partial에서 기존 파일을 덮어쓰지 않는 방식으로 원자 발행하고 실패 시 이번 작업 산출물만 정리하도록 강화했습니다.
- 문서 작업에 품질·저장 프로필·native drag/drop·경고 필터와 한국어 설명을 연결하고 아직 처리하지 않는 hidden/wrap 옵션은 비활성화해 명확히 표시했습니다.
- 스캔 PDF의 OCR 좌표와 신뢰도가 재조립에서 누락되지 않도록 수정하고, 대형 문서는 페이지 PNG spool을 사용해 메모리 사용량이 페이지 수에 따라 증가하지 않도록 변경했습니다.
- PDF 번역 배경을 페이지 렌더에서 판정하고 복잡한 페이지는 전체 래스터 fallback을 사용하며, 결과 전체 페이지를 순차 렌더해 크기와 회전을 다시 확인하도록 강화했습니다.
- 문서 진행·경고·완료·실패 알림을 하나의 형식으로 통합하고 보고서 경고 위치에 키보드 focus가 가능한 고유 anchor를 추가했습니다.
- 복잡한 일반 PDF 페이지를 해당 페이지만 300DPI로 다시 처리하고, page/job spool 용량과 디스크 여유 공간을 작업 전에 제한하도록 강화했습니다.
- PDF 결과의 회전 크기를 추측하지 않고 운영체제 renderer가 만든 원본·결과 bitmap을 직접 비교하도록 수정했습니다.
- PDF 페이지·PPTX 슬라이드·명시된 XLSX 셀 경고를 결과 위치 미리보기로 이동하고, 정확한 위치를 알 수 없는 항목은 보고 항목 이동으로 구분했습니다.
- 번역 batch 재개 참조를 저장 단계까지 유지하고 암호화 저장소의 명시적 ACK가 없는 작업 임시 파일은 성공·취소·오류 후 안전하게 정리하도록 변경했습니다.
- 경고 위치를 클릭하면 발행된 결과 파일을 다시 읽어 PDF 페이지 이미지, 프레젠테이션 도형, 스프레드시트 인접 셀 또는 DOCX 문맥을 실제 미리보기로 표시하도록 변경했습니다.
- 문서 재개 checkpoint의 완료 batch cursor와 암호화 결과 참조를 실제 번역 흐름에서 소비하고, PDF는 임시 bitmap 대신 검증된 원본을 다시 렌더하도록 변경했습니다.
- PDF 임시 bitmap은 사용자 앱 데이터 폴더에서만 만들고 보존 확인 여부와 관계없이 모든 종료 경로에서 즉시 삭제하도록 강화했습니다.
- XLSX shared string과 inline string 경고가 실제 `sheet:<이름>/cell:<A1>` 위치를 가리키고, 여러 셀이 같은 shared string을 쓰는 경우 대표 셀과 사용 수를 함께 유지하도록 수정했습니다.
- DOCX/PPTX/XLSX/PDF 세그먼트 ID를 소스 fingerprint와 형식·part/page·안정 위치에서 만드는 결정적 UUID로 통일해 재검사와 재개 시 같은 ID를 사용하도록 수정했습니다.
- Office 결과 미리보기가 패키지 전체를 해제하지 않고 필요한 part만 항목당 16MiB·합계 64MiB 안에서 읽으며, 초과는 별도 제한 오류로 거부하도록 강화했습니다.

- DOCX, PPTX, XLSX의 텍스트를 기존 run, 표, 슬라이드, 노트, 셀, 관계와 미디어를 유지하며 번역하고 원본 옆에 새 파일로 저장하는 문서 작업 화면을 추가했습니다.
- 문서 번역에 대상 언어와 댓글·발표자 노트 옵션, 단계별 진행·취소, 결과 재열기 검증, 레이아웃 위험 보고와 결과 파일 열기를 추가했습니다.

- Windows.Media.Ocr와 macOS Vision을 이용한 로컬 이미지 OCR, 좌표를 유지한 ChatGPT/Codex 블록 번역과 원위치 이미지 렌더링을 추가했습니다.
- 이미지 번역 결과의 원본·번역·나란히 미리보기, 블록별 수정·표시·재시도, 진행·취소와 PNG/JPEG/WebP 새 파일 저장을 추가했습니다.

- PNG/JPEG/WebP/TIFF/BMP 이미지 가져오기와 Windows/macOS 화면 영역 선택 작업 화면을 추가했습니다. 다중 모니터·배율 좌표를 보존하고 원본은 변경하지 않은 채 다음 OCR 단계로 전달합니다.
- 화면 영역 선택에 고정된 모니터 미리보기, 드래그, 키보드 이동·크기 조절, Enter 확인, Escape 취소와 한국어·영어 안내를 추가했습니다.
- Windows 트레이와 macOS 메뉴 막대에서 빠른 번역, 전체 창, 설정, 단축키 일시 중지, 상태·계정 확인과 안전한 종료를 사용할 수 있습니다.
- 설치 앱의 로그인 시 실행, 닫기 동작과 빠른 접근 위치를 설정에서 즉시 변경할 수 있습니다.
- SmartCAT Translate의 Tauri 데스크톱 앱과 텍스트 번역 작업 영역 기본 화면을 추가했습니다.
- 텍스트 번역 작업 영역의 영어 표시 및 접근성 레이블 경로를 추가했습니다.
- Windows와 macOS 번들용 앱 아이콘 자산을 추가했습니다.
- ChatGPT 계정 연결, 브라우저 로그인·취소, 사용률과 현지 시간 기준 초기화 시각 표시를 추가했습니다.
- 앱 시작 시 설치기에 포함된 SmartCAT 전용 Codex 0.144.4 sidecar의 체크섬과 동일 세션 보안 증명을 확인한 뒤 ChatGPT 계정 화면을 실제 App Server에 연결합니다.
- ChatGPT 계정 화면의 한국어·영어 전체 표시와 로그인 재개·취소 경합 복구를 추가했습니다.
- ChatGPT 계정의 Codex App Server로 텍스트를 번역하는 제한된 스트리밍 백엔드를 추가했습니다. 원문은 메모리 전용 임시 스레드에서만 처리하고 도구 실행은 차단하며, 요청 창에만 번역 진행 결과를 전달합니다.
- 한국어·영어 설정 화면에 번역 프로필, 기본 언어쌍, 품질·문체·분야, 용어집과 보호 용어, 계정에서 제공되는 Codex 모델 선택, 테마와 앱 동작 설정을 추가했습니다.
- 저장한 모델을 더 이상 계정에서 사용할 수 없으면 원래 선택을 경고용으로 유지하면서 실제 작업은 자동 선택으로 안전하게 전환합니다.
- 원문과 대상 언어가 같으면 자동으로 문장을 바꾸지 않고 문장 개선 또는 대상 언어 변경을 선택하도록 안내합니다.
- 모델 목록을 읽지 못해도 저장한 모델 선택을 유지하고 상태를 안내하며, 권위 있는 목록을 확인한 경우에만 사용할 수 없는 모델을 자동 선택으로 전환합니다.
- 계정 연결 상태가 바뀌면 설정 화면의 모델 목록을 다시 확인하고, 일시적인 조회 오류에는 한국어·영어 재시도 동작을 제공합니다.
- DeepL과 유사한 두 칸 텍스트 작업 화면에 언어쌍 전환, 실시간 번역 표시, 취소, 재시도, 복사, 텍스트 파일 저장과 전체 지우기를 추가했습니다.
- 기본 번역 프로필과 보호 용어집을 실제 번역 요청에 적용하며, 같은 언어쌍은 번역 대신 문장 개선을 선택하도록 안내합니다.
- 넓은 창과 작은 창에서 자동으로 재배치되는 한국어·영어 화면, 키보드 번역·취소와 화면 읽기 상태 안내를 추가했습니다.
- 번역 화면에서 설정 화면으로 바로 이동할 수 있는 한국어·영어 탭과 저장된 밝게·어둡게·시스템 테마 적용을 추가했습니다.
- 운영체제의 Windows·macOS 저장 대화상자로 번역문을 저장하고 취소·성공·실패를 내용 없는 고정 상태로 안내합니다.
- 번역 또는 취소 정리가 진행되는 동안 설정 이동을 잠그고 한국어·영어로 이유를 안내하며, 작업이 끝나면 설정을 다시 사용할 수 있습니다.
- 키보드 초점을 유지한 채 원문을 읽기 전용으로 보호하고, 번역 직후 Escape로 안전하게 취소할 수 있습니다.
- 운영체제 저장 대화상자의 제목과 파일 유형을 검증된 한국어·영어 값으로 표시합니다.
- Playwright 1.62.1로 실제 번역 요청 계약과 넓은 창·작은 창 배치를 검사하는 브라우저 시험을 CI에 추가했습니다.
- 구성한 전역 단축키로 선택한 텍스트를 안전하게 가져와 재사용 가능한 작은 번역 창에서 실시간 번역하도록 연결했습니다.
- 빠른 번역 창에 언어쌍·프로필, 취소·재시도·복사·로컬 듣기·고정·Escape 닫기·전체 창 열기와 커서 주변 다중 모니터 배치를 추가했습니다.

### 보안

- 번역용 Codex를 앱 전용 홈과 빈 작업공간에 격리하고, 사용자 구성·지시 파일·도구 기능이 상속되지 않도록 강화했습니다. 취소·창 종료·비정상 프로토콜 경합은 작업 시작 전 또는 제한된 정리 시간 안에 실패 폐쇄됩니다.
- 번역용 하류 Codex는 최종 모델 요청에서 도구 선언을 항상 0개로 만들고 사용자·프로젝트 지시문 탐색을 소스 수준에서 끕니다. stock·변조 런타임과 런타임 다운로드 폴백은 거부합니다.
- 앱 설정은 전용 JSON 파일에 완전 기록·동기화한 임시 파일을 원자 교체해 저장합니다. 저장 실패 전에는 보이는 값을 바꾸지 않고, 저장 작업과 앱 종료 경합을 직렬화합니다.
- 번역 전 모델 확인은 번역 작업의 동일한 전체 제한시간과 취소 경계 안에서만 수행합니다. 창 종료나 앱 종료는 대기 중인 모델 조회를 즉시 정리하고, 특정 모델은 확인 실패 시 중단하며 자동 선택만 서버 기본값으로 안전하게 전환합니다.
- 텍스트 화면은 서버와 같은 원문 크기 한도를 사전에 검사하고, 작업 ID가 다른 이벤트와 완료 뒤 늦게 온 이벤트를 무시하며, 화면 종료 시 진행 중인 작업과 이벤트 구독을 정리합니다.
- 저장한 번역 분야와 원문·대상 용어 매핑은 원문과 함께 명시적인 비신뢰 번역 데이터로만 전달하며, 항목별·합계 크기를 제한하고 도구·파일·명령 동작을 금지합니다.
- 번역 이벤트 구독이 준비되기 전에는 작업을 시작하지 않으며, 원문·언어쌍 변경과 취소 실패 경합에서 오래된 결과를 표시하거나 새 작업을 시작하지 않습니다.
- Tauri 화면에 IPC와 앱 자산만 허용하고 실행 객체·프레임·폼 전송을 막는 콘텐츠 보안 정책을 적용했습니다.
- 명시적 릴리스 작업은 고정된 표준 도구로 Codex와 앱의 잠금 의존성 그래프, 전체 작업공간, sidecar와 모든 설치 파일의 SBOM·SHA-256을 검증한 뒤에만 동일 산출물의 GitHub provenance를 만들고 업로드합니다.
- 문서 재개 plaintext와 retention token/ACK를 공개 IPC에서 제거했습니다. 재시도 payload의 일회용 token과 내용 없는 checkpoint는 backend-only consumer에만 전달하고, consumer가 없으면 즉시, 그 밖에는 최대 60초 또는 begin/완료/취소 시 번역문을 zeroize한 뒤 제거합니다.
- 로컬·개발 빌드의 업데이트 플러그인은 빈 공개키와 빈 주소 목록으로 안전하게 초기화되어 앱 시작 오류 없이 실패 폐쇄되며, 실제 업데이트 주소와 공개키는 보호된 서명 릴리스에서만 주입됩니다.

### 배포

- Windows x64용 unsigned MSI와 NSIS 설치 파일을 실제 생성하고 C 드라이브 사용자 앱 폴더 설치·실행을 확인했습니다. 설치된 Codex 0.144.4 sidecar 버전, runtime/installer SHA-256, CycloneDX SBOM과 산출물 증빙을 검증했습니다.
- 런타임 자산 검사가 생성 매니페스트의 canonical `binary` 필드를 사용하고 단순 파일명만 허용하도록 수정했습니다.

### 설계

- Windows와 macOS용 Tauri 2 기반 번역 앱의 전체 설계를 승인했습니다.
- Codex App Server를 통한 ChatGPT 계정 인증 방식을 확정했습니다.
- 텍스트, 이미지, 화면 캡처, DOCX, PPTX, XLSX, PDF 번역 범위를 확정했습니다.
- 전역 단축키, 충돌 검사, 대체 단축키 추천 방식을 확정했습니다.
- 로컬 암호화 기록, 시크릿 번역, 복구 및 자동 검증 정책을 확정했습니다.


## 2026-09-01 번역·Windows 안내·테마 및 로그인 복구

### 수정

- 텍스트 편집은 계정과 이벤트 수신기가 준비된 경우 500ms 디바운스 뒤 정확히 한 번 자동 번역을 시작합니다. IME 조합, 빈 값·한도 초과·동일 언어, 실행 중인 작업·정리 대기, 수동 실행과의 중복은 차단합니다.
- 붙여넣기 즉시 번역의 기존 임시 스레드 동작을 유지하면서, 저장된 기본 스레드를 포크할 수 없으면 내용 없는 새 임시 스레드로 복구합니다. 수동 번역과 정리된 백엔드 오류 코드도 그대로 전달합니다.
- Windows에서는 macOS 접근성 안내와 Bundle ID 입력을 표시하지 않고 Windows 단축키 안내와 실행 파일 이름을 표시합니다. 플랫폼 판별은 공용 유틸리티로 통합했습니다.
- 신규 설정과 암묵적 레거시 테마는 밝은 테마로 시작하고 초기 화면의 어두운 깜박임을 없앴습니다. 명시적 밝음·어두움과 스키마 2의 명시적 시스템 선택은 보존합니다.
- ChatGPT 로그인 명령은 Codex 계정 런타임 준비를 기다리며 종료·부트스트랩 실패 시 대기 요청을 안전하게 해제합니다. 공식 HTTPS `chatgpt.com` 경계, 정리된 오류 코드, URL·비밀·원문 비기록 정책은 유지합니다.
- 계정 런타임 준비 알림은 각 대기자가 상태 확인 전에 Tokio 알림 future를 등록하도록 수정했습니다. 부트스트랩 완료·실패 또는 종료 알림이 상태 확인과 첫 await 사이에 발생해도 유실되지 않으며 여러 동시 대기자를 즉시 해제합니다.
- Windows 체크아웃에서도 단축키 카탈로그 무결성 해시가 줄바꿈 변환으로 깨지지 않도록 해당 JSON을 LF로 고정했습니다.

### 개발 절차

- CLI OMX 구현·최종 합성 전용 계약과 정지된 실행 판정 규칙을 자동 검사하는 무의존성 Node 테스트를 `records:test` 검증에 연결했습니다.
- 강제 푸시 기록 검사의 대체 기준은 대상 커밋과의 병합 기준을 계산해 비교합니다. 병합 기준이 없거나 대상 자체로 해석되면 빈 비교로 우회하지 않고 실패하며, 분기된 기본 브랜치의 최신 변경은 기능 브랜치 기록 검사에 섞지 않습니다.
- 명시 기준과 대체 기준이 모두 유효하지 않은 경우, 대체 기준이 대상과 같은 경우, 대체 기준이 대상과 분기된 경우를 명령 수준 회귀 검사로 고정했습니다.
- 중단된 앱 서버 에이전트는 OS 자식 빌드 프로세스가 없음을 확인한 뒤 실패로 분류하고, 현재의 감독된 CLI OMX 작업에서 후보 통합·수정·검증을 재개했습니다.
- 이 작업은 설치·릴리스·배포·푸시를 수행하지 않습니다. 최종 단일 구현 커밋은 현재 브랜치에만 만들고 독립 검토 뒤 컨트롤러가 푸시합니다.
- GitHub Actions PR 실행 `33469992956`의 Windows package 작업 `99738140137`에서 `patch_hash_mismatch`로 중단된 SmartCAT Codex 런타임 빌드를 수정했습니다. Git 인덱스의 감사 패치 SHA-256은 핀과 일치했지만 Windows 체크아웃의 CRLF 변환이 작업 트리 바이트를 바꾼 것이 원인이었습니다.
- 모든 `*.patch` 파일을 `text eol=lf`로 체크아웃하도록 고정하고, 감사 패치의 작업 트리 SHA-256이 `pin.json`의 `patchSha256`과 일치하는지 런타임 빌드 회귀 검사에서 함께 검증합니다.
