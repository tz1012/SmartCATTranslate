# Task 9 구현·검토 보고서 — 전체 텍스트 번역 작업 화면

날짜: 2026-08-30
상태: 구현 완료, 검토 수정 1 전체 관문 통과

## 제공 기능

- 작은 DeepL형 원문·결과 작업 화면, 자동 감지와 다국어 언어쌍, 언어 전환, 번역·취소·재시도·복사·지우기를 연결했다.
- 번역 조각과 완료·실패 상태를 현재 작업에만 적용하고, 같은 언어이면 자동 변환하지 않고 문장 개선 또는 대상 언어 변경을 제안한다.
- 한국어·영어 번역/설정 탭, 저장된 테마, 화면 읽기 탭·패널 관계, 620픽셀 기준 데스크톱·작은 창 재배치를 제공한다.
- 저장한 기본 프로필의 품질·문체·분야, 보호 전용 용어와 원문·대상 용어 매핑을 실제 번역 요청에 전달한다.
- 번역문은 브라우저 다운로드가 아니라 Windows·macOS 운영체제 저장 대화상자로 새 텍스트 파일에 저장한다.

## 보안과 일관성

- 원문·분야·용어집은 메시지 끝까지 이어지는 단일 JSON 비신뢰 번역 데이터다. 데이터 안의 지시나 경계 표식처럼 보이는 문자열을 실행하지 않고 도구·명령·파일 접근과 그 밖의 동작을 금지한다.
- 보호 용어와 용어 매핑은 각 항목의 문자·바이트 크기, 항목 수와 합계 64 KiB를 Rust 신뢰 경계에서 검사한다.
- 이벤트 구독이 준비되기 전에는 번역을 시작하지 않는다. 구독 실패는 원격 세부 문구 없는 고정 한국어·영어 오류로 닫힌다.
- 진행 중에는 원문·언어쌍을 바꿀 수 없다. 완료 뒤 원문·언어쌍을 바꾸면 결과와 감지 언어를 함께 제거해 오래된 번역을 새 원문에 연결하지 않는다.
- 이미 시작한 작업은 계정이 로그아웃되어도 취소할 수 있다. 취소 거부·오류에는 작업 식별자를 유지해 정리를 다시 시도하고, 정리가 확인되기 전 새 번역과 편집을 막는다.
- 복사·선택·파일 쓰기 실패는 내용과 경로가 없는 고정 오류만 표시한다. 번역 원문·번역문·원시 이벤트·계정 정보는 로그, 자동 작업 기록과 앱 기록에 저장하지 않는다.
- Tauri 콘텐츠 보안 정책은 자체 자산, IPC와 개발 연결만 허용하고 실행 객체·프레임·폼 전송을 차단한다.

## TDD 증거

- RED: 분야·용어 매핑 직렬화 계약 부재, 종료 표식처럼 보이는 원문, 용어 합계 한도 초과, 이벤트 구독 전 시작, 구독 실패, 로그아웃 뒤 취소, 취소 거부·오류 정리, 완료 뒤 원문 수정, 브라우저 Blob 저장, 복사·저장 실패, 설정 화면 미마운트와 탭 관계를 실패 시험으로 재현했다.
- GREEN: Rust 직렬화·프롬프트·입력 경계와 네이티브 저장 시험, React 요청·상태·접근성 시험, Playwright 실제 요청·스트림·복사·반응형 시험을 구현과 함께 통과시켰다.
- E2E 최초 실패는 Playwright 브라우저 실행 파일 미설치였다. exact 1.62.1 브라우저를 프로젝트 전용 캐시에 설치하고, CI도 운영체제·exact 버전·잠금 파일 캐시 키와 명시적 설치 단계를 사용하도록 고쳤다.

## 검증 결과

- React/Vitest 전체: 55/55 통과
- TypeScript/Vite 프로덕션 빌드: 통과
- Playwright 1.62.1 데스크톱·모바일: 2/2 통과
- Rust 전체: 최종 보강 포함 134/134 통과, 번역 조정기 집중 시험 33/33 통과
- rustfmt와 전체 대상·시험 기능 엄격 Clippy: 통과
- 운영 런타임·공급망 계약: 19/19 통과
- 자동 기록 계약: 16/16 통과

## 열린 우려

- 네이티브 저장 대화상자의 선택·취소·쓰기 로직은 Rust 모형과 실제 Tauri 컴파일 경계로 검증했다. 공개 설치 전 Windows와 macOS 번들에서 각각 한 번씩 실제 대화상자 육안 시험이 필요하다.
- 브라우저 E2E는 실제 프런트엔드 번들과 Tauri IPC 모형을 사용한다. 실제 ChatGPT 계정 네트워크 호출은 하지 않았으며 계정·런타임 보안 수명주기는 기존 Rust 통합 시험으로 유지했다.
- 이미지·문서·화면 캡처와 전역 단축키는 승인된 후속 작업 범위이며 이번 Task 9에서는 비활성 안내 탭만 유지했다.
